public class InputOutputMain {
    // io can be performed either using byte or character data - methods are similar, but classes will vary
    // character data will look meaningful if you open the file (primitive char, unicode character)
    // binary data however (byte) will doesn't look meaningful when opening (primitive byte, number from -128 to 127)

    // files may also be serial/sequential or random access
    // sequencial: stream of data arriving at program in a defined order following in sequence
    // random access: access data in random pattern
    public static void main(String[] args) {
    }
}
