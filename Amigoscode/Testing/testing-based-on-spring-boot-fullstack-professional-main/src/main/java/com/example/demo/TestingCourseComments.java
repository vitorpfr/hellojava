package com.example.demo;

public class TestingCourseComments {
    // Junit: testing framework in java (current version is 5)
    // assertJ: library that provides better assertion functions than Junit (usage is optional, you can also use junit's Assertion class static methods)

    // Having spring-boot-starter-test as a dependency in the pom.xml already adds all required dependencies for testing (JUnit, assertJ(optional), mockito, etc)
}
